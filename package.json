{
  "name": "vending-machines",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "backend": "yarn workspace backend start:dev",
    "frontend": "yarn workspace frontend start",
    "start": "concurrently --kill-others-on-fail \"yarn workspace @vending/types build\" \"yarn backend\"  \"yarn frontend\""
  },
  "engines": {
    "node": "16.13.2",
    "yarn": "1.22.18"
  },
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}
